[project]
name = "wordmixer"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.2",
    "selenium>=4.40.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "selenium>=4.40.0",
]
